import React from "react";

// Temporary stub to satisfy typecheck; replace with real implementation later.
export function ThemeToggle() {
  return (
    <button type="button" aria-label="Toggle theme" className="inline-flex items-center gap-2 rounded px-2 py-1 text-sm">
      <span>Theme</span>
    </button>
  );
}